# Oscar Service Configuration Example
# Скопируйте этот файл в src/main/resources/application.properties и настройте под ваш сервер

# ===========================================
# Server Configuration
# ===========================================
# Для локального запуска:
server.host=localhost
server.port=9001

# Для деплоя на продакшн сервер, измените на:
# server.host=your-server-hostname.com
# server.port=80

server.context.service1=service1
server.context.service2=service2

# ===========================================
# Movie Service (service1) API Configuration
# ===========================================
# URL первого сервиса (movie-service)
# Формируется автоматически из параметров выше
movie.service.base.url=http://${server.host}:${server.port}/${server.context.service1}/api/v1

# Для продакшена можно указать явно:
# movie.service.base.url=http://your-server-hostname.com/service1/api/v1

# ===========================================
# Logging Configuration
# ===========================================
logging.level.root=INFO
logging.level.ru.itmo.soa=DEBUG

# ===========================================
# Jackson Configuration
# ===========================================
jackson.serialization.write-dates-as-timestamps=false
jackson.deserialization.fail-on-unknown-properties=false
jackson.default-property-inclusion=non_null

# ===========================================
# Encoding
# ===========================================
http.encoding.charset=UTF-8
http.encoding.enabled=true
http.encoding.force=true

